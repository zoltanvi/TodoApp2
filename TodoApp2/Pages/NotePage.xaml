<local:BasePage
    x:Class="TodoApp2.NotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:TodoApp2"
    xmlns:core="clr-namespace:TodoApp2.Core;assembly=TodoApp2.Core"
    Title="NotePage"
    d:DesignHeight="450"
    d:DesignWidth="400"
    x:TypeArguments="core:NotePageViewModel"
    mc:Ignorable="d">

    <Grid>

        <avalonEdit:TextEditor
            xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
            Name="textEditor"
            WordWrap="{Binding ApplicationViewModel.ApplicationSettings.NotePageWordWrap, Source={x:Static local:ViewModelLocator.Instance}, UpdateSourceTrigger=PropertyChanged}"
            FontFamily="{Binding Path=ApplicationViewModel.ApplicationSettings.NoteFontFamily, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource enumToFontFamilyConverter}}"
            FontSize="{Binding Path=ApplicationViewModel.ApplicationSettings.TaskFontSizeDouble, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource fontSizeScaler}}"
            TextChanged="textEditor_TextChanged"
            Background="Transparent"
            Foreground="{DynamicResource ForegroundBrush}"
            LineNumbersForeground="{DynamicResource LineNumberBrush}"
            ShowLineNumbers="True">
            <avalonEdit:TextEditor.Style>
                <Style TargetType="avalonEdit:TextEditor">
                    <Style.Resources>
                        <Style BasedOn="{StaticResource DarkScrollBarStyle}" TargetType="{x:Type ScrollBar}" />
                    </Style.Resources>
                </Style>
            </avalonEdit:TextEditor.Style>

        </avalonEdit:TextEditor>

        <Border
            Background="{DynamicResource PageBgBrush}"
            BorderBrush="black"
            Visibility="{Binding ApplicationViewModel.ApplicationSettings.NotePageWordWrap, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityNegatedConverter}}"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Width="{Binding UIScaler.NotePageBoxWidth, Source={x:Static local:ViewModelLocator.Instance}}"
            Height="{Binding UIScaler.NotePageBoxWidth, Source={x:Static local:ViewModelLocator.Instance}}" />

    </Grid>
</local:BasePage>