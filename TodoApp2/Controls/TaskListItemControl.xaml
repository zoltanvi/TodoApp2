<UserControl x:Class="TodoApp2.TaskListItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TodoApp2"
             mc:Ignorable="d" 
             MinHeight="60"
             d:DesignHeight="60" d:DesignWidth="350">

    <Grid d:DataContext="{x:Static local:TaskListItemDesignModel.Instance}" Background="#1f1f1f">
        <Border Background="{StaticResource TaskItemBackgroundBrush}"
                Margin="16 3"
                CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <!--Color bar column-->
                    <ColumnDefinition Width="8"/>
                    <!--Checkbox column-->
                    <ColumnDefinition Width="36"/>
                    <!--Main content column-->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!--Color bar-->
                <Border Grid.Column="0"
                        CornerRadius="5 0 0 5"
                        Background="{Binding Color, Converter={local:StringRGBToBrushConverter}}"/>

                <CheckBox Grid.Column="1"
                          Content="asdasd"
                          IsChecked="{Binding IsDone}"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center" Style="{StaticResource TaskCheckBoxStyle}" />

                <!--Main content-->
                <Grid Grid.Column="2" Margin="6">
                    <Grid.RowDefinitions>
                        <!--Task description row-->
                        <RowDefinition Height="*"/>
                        <!--Timestamp row-->
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!--Task description-->
                    <TextBlock Grid.Row="0"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"
                               HorizontalAlignment="Stretch"
                               FontSize="16"
                               FontFamily="Segoe UI"
                               Foreground="{StaticResource DescriptionForegroundBrush}"
                               Text="{Binding Content}"
                               
                               />

                    <!--Timestamp-->
                    <!--<TextBlock Grid.Row="1" 
                               Text="2020-04-22 20:57"
                               Foreground="{StaticResource TimestampForegroundBrush}"
                               />-->

                </Grid>

            </Grid>
        </Border>
    </Grid>


</UserControl>
