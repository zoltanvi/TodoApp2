<UserControl
    x:Class="TodoApp2.RichTextEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TodoApp2"
    xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
    mc:Ignorable="d"
    d:DesignHeight="450"
    x:Name="RichTextEditorUC"
    d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Row="0"
                Grid.Column="0"
                Background="Transparent"
                BorderBrush="{DynamicResource Elevation_2}"
                BorderThickness="0 0 0 1"
                Visibility="{Binding IsChecked, ElementName=IsEditorOpenToggle, Converter={StaticResource boolToVisibilityConverter}}" />

            <Border
                x:Name="ToolBarBorder"
                Grid.Row="0"
                Background="Transparent"
                Grid.Column="0"
                IsEnabled="{Binding ElementName=RichTextEditorUC, Path=IsReadOnly, Converter={StaticResource boolNegatedConverter}}">
                <DockPanel>
                    <WrapPanel
                        Visibility="{Binding IsChecked, ElementName=IsEditorOpenToggle, Converter={StaticResource boolToVisibilityConverter}}"
                        Background="#00ffffff"
                        DockPanel.Dock="Top"
                        ToolBarTray.IsLocked="True">

                        <ToggleButton
                            Command="{Binding SetBoldCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconBold}"
                            IsChecked="{Binding IsSelectionBold, ElementName=PART_TextEditor}"
                            Style="{DynamicResource TextEditorToggleButtonStyle}"
                            Margin="0 0 0 4"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQABold, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Bold (Ctrl + B)" />
                        <ToggleButton
                            Margin="2 0 0 2"
                            Command="{Binding SetItalicCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconItalic}"
                            IsChecked="{Binding IsSelectionItalic, ElementName=PART_TextEditor}"
                            Style="{DynamicResource TextEditorToggleButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQAItalic, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Italic (Ctrl + I)" />
                        <ToggleButton
                            Margin="2 0 0 2"
                            Command="{Binding SetUnderlinedCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconUnderlined}"
                            IsChecked="{Binding IsSelectionUnderlined, ElementName=PART_TextEditor}"
                            Style="{DynamicResource TextEditorToggleButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQAUnderlined, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Underline (Ctrl + U)" />

                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding SetSmallFontSizeCommand, ElementName=PART_TextEditor}"
                            Content="S"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            FontFamily="Segoe UI"
                            FontWeight="Bold"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQASmall, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Small font" />
                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding SetMediumFontSizeCommand, ElementName=PART_TextEditor}"
                            Content="M"
                            FontFamily="Segoe UI"
                            FontWeight="Bold"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQAMedium, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Medium font" />
                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding SetBigFontSizeCommand, ElementName=PART_TextEditor}"
                            Content="L"
                            FontFamily="Segoe UI"
                            FontWeight="Bold"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQALarge, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Large font" />

                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding IncreaseFontSizeCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconFontSizeIncrease}"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQAIncrease, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Increase size (Ctrl + [OemCloseBrackets])" />
                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding DecreaseFontSizeCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconFontSizeDecrease}"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQADecrease, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Decrease size (Ctrl + [OemOpenBrackets])" />

                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding MonospaceCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconMonospace}"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQAMonospace, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Monospace font" />

                        <Button
                            Margin="2 0 0 2"
                            Command="{Binding ResetFormattingCommand, ElementName=PART_TextEditor}"
                            Content="{DynamicResource SegoeIconDeleteFormatting}"
                            Style="{DynamicResource TextEditorButtonStyle}"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQAReset, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Reset all formatting (Ctrl + H)" />

                        <local:ColorPickerComboBox
                            Margin="2 0 0 2"
                            AppliedColor="{Binding TextColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Focusable="False"
                            ItemContainerStyle="{StaticResource TaskItemColorPickerItemContainerStyle}"
                            ItemTemplate="{StaticResource TaskItemColorPickerItemTemplate}"
                            ItemsSource="{Binding ColorListProvider.Items, Source={x:Static local:ViewModelLocator.Instance}}"
                            Style="{DynamicResource RichTextEditorColorPickerButtonStyle}"
                            Background="Transparent"
                            IsEnabled="{Binding IsChecked, ElementName=IsEditorOpenToggle}"
                            Visibility="{Binding AppSettings.TextEditorQATextColor, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource boolToVisibilityConverter}}"
                            ToolTip="Change color (Ctrl + G)" />

                    </WrapPanel>
                </DockPanel>
            </Border>

            <!--  Main content  -->
            <Border
                x:Name="border"
                Grid.Row="1"
                Grid.Column="0"
                Margin="0 0 5 0"
                Background="Transparent"
                SnapsToDevicePixels="True">

                <!--
                    IMPORTANT: FocusSetter Attached property must be
                    after Visibility property (if defined) in order to work!
                    
                    Focusable property is needed to exit edit mode on enter keypress.
                -->
                <local:FormattableTextEditorBox
                    x:Name="PART_TextEditor"
                    RenderOptions.ClearTypeHint="Enabled"
                    DocumentContent="{Binding DocumentContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Foreground="{DynamicResource ForegroundBrush}"
                    Style="{DynamicResource BaseRichTextBoxStyle}"
                    Opacity="{Binding TextOpacity}"
                    IsReadOnly="{Binding ElementName=RichTextEditorUC, Path=IsReadOnly}"
                    Background="Transparent"
                    EnterAction="{Binding EnterAction}"
                    Focusable="{Binding Focusable}"
                    local:FocusSetter.Value="{Binding NeedFocus}"
                    TextColor="{Binding TextColor}"
                    AcceptsTab="{Binding AcceptsTab}"
                    ScrollViewer.CanContentScroll="False"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <Behaviors:Interaction.Triggers>
                        <Behaviors:EventTrigger EventName="LostFocus">
                            <Behaviors:InvokeCommandAction Command="{Binding LostFocusCommand}" />
                        </Behaviors:EventTrigger>
                    </Behaviors:Interaction.Triggers>

                    <Behaviors:Interaction.Behaviors>
                        <local:PutCaretAtTheEndRTBBehavior />
                    </Behaviors:Interaction.Behaviors>

                    <local:PushBindingManager.PushBindings>
                        <local:PushBinding
                            TargetProperty="IsEmpty"
                            Path="IsContentEmpty"
                            Mode="OneWayToSource" />
                    </local:PushBindingManager.PushBindings>

                </local:FormattableTextEditorBox>
            </Border>

            <!--  Editor toggle button  -->
            <Border
                Grid.RowSpan="2"
                Grid.Column="1"
                Width="{Binding UIScaler.TextEditorToggleWidth}"
                HorizontalAlignment="right"
                IsEnabled="{Binding ElementName=RichTextEditorUC, Path=IsReadOnly, Converter={StaticResource boolNegatedConverter}}">

                <Border.Visibility>
                    <MultiBinding>
                        <MultiBinding.Converter>
                            <local:BoolToVisibilityOrConverter />
                        </MultiBinding.Converter>
                        <Binding Path="IsEditMode" />
                        <Binding Path="IsFocused" ElementName="PART_TextEditor" />
                    </MultiBinding>
                </Border.Visibility>

                <ToggleButton
                    x:Name="IsEditorOpenToggle"
                    Margin="0 2 0 0"
                    Foreground="{DynamicResource ForegroundBrush}"
                    IsChecked="{Binding IsToolbarOpen}"
                    Style="{DynamicResource TextEditorArrowToggleButtonStyle}" />
            </Border>

            <!--  Watermark text  -->
            <TextBlock
                x:Name="PART_placeholder"
                Grid.Row="1"
                Grid.Column="0"
                Margin="2 0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="{DynamicResource PlaceholderBrush}"
                IsHitTestVisible="False"
                FontFamily="{Binding Path=AppSettings.FontFamily, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource enumToFontFamilyConverter}}"
                FontSize="{Binding Path=AppSettings.TaskFontSizeDouble, Source={x:Static local:ViewModelLocator.Instance}, Converter={StaticResource fontSizeScaler}}"
                Visibility="{Binding IsEmpty, ElementName=PART_TextEditor, Converter={StaticResource boolToVisibilityConverter}}"
                Text="{Binding WatermarkText}" />

        </Grid>
    </Grid>
</UserControl>
